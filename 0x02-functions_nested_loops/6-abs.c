#include "main.h"
/**
 * _abs - Entry point.
 * @i: is all integer numbers.
 * Descriptin: computes the absolute value of an integer.
 * Return: 0 Alwayes Success.
 */
int _abs(int i)
{
	if (i < 0)
	{
	return (i * -1);
	}
	else
	{
		return (i);
	}
}
